2/6/26 Lab 3 Notes
pwd means print working directory
ls means list and will show everything that is in your directory
clear clears the terminal
control C will cancel whatever you were currently typing for if you get stuck
cd and then wherever you want to go helps you travel in the directory
to specify options for ls, you do ls - and then after the dash put a letter (-F would show files)
man means manual and it means the next command after that will show the operators manual for that command
ls -lrth shows a bunch of information about the directories
use tab to autofill commands
use head and then the directory to see the beginnning of it
cat prints the whole file to the screen
to get out of the directory, us cd ../
cd ../ is stackable, you can keep doing it to get to the home directory
a shortcut to get back to the home directory is cd ~
2/13/26 Lab 4 Notes
Change directories by typing cd and then the directory you want to go to
Typing cd by itself goes straight to your home directory
The * is the wildcard
Doing ls -laF gives the (l) long format, (a) all of it, (F) and the format
You can rerun a code from history by typing history, hit enter, then do ! followed by the number with the command
grep looks through a file and the first thing you put after grep is what you are searching for (might want to use quotes)
less looks through files and lets you scroll through it rather than printing it across the whole screen
to quit, hit q
cp means copy
mkdir means make directory
2/20/26 Lab 5 Notes
used ls -lh to find the size of the file in human readable format
K means kilobytes when used as a unit
to do an absolute path, start out with the forward slash (ex: ls -lh /home/amb1589/)
rwx means read write execute
for drwxrwxrwx the file type is the first letter, the first rwx is the user, the second is the group, and the third is other
chmod means change mode
conda activate genomics activates genomics
conda deactivate deactivates it